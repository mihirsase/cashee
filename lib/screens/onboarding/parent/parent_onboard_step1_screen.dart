import 'package:flutter/material.dart';

class ParentOnboardingStep1Screen extends StatelessWidget {
  const ParentOnboardingStep1Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Parent Onboarding Step 1 Screen")),
    );
  }
}
