import 'package:flutter/material.dart';

class TeenOnboardingStep1Screen extends StatelessWidget {
  const TeenOnboardingStep1Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Teen Onboarding Step 1 Screen")),
    );
  }
}
