import 'package:flutter/material.dart';

class ParentSignupScreen extends StatelessWidget {
  const ParentSignupScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Parent Signup Screen")),
    );
  }
}
