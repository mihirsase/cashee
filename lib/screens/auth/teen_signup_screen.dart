import 'package:flutter/material.dart';

class TeenSignupScreen extends StatelessWidget {
  const TeenSignupScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Teen Signup Screen")),
    );
  }
}
