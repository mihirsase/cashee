import 'package:flutter/material.dart';

class ParentTipsScreen extends StatelessWidget {
  const ParentTipsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Parent Tips Screen")),
    );
  }
}
