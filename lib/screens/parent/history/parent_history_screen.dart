import 'package:flutter/material.dart';

class ParentHistoryScreen extends StatelessWidget {
  const ParentHistoryScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Parent History Screen")),
    );
  }
}
