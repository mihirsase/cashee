import 'package:flutter/material.dart';

class TeenHubScreen extends StatelessWidget {
  const TeenHubScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Teen Hub Screen")),
    );
  }
}
