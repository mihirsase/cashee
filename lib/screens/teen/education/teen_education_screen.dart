import 'package:flutter/material.dart';

class TeenEducationScreen extends StatelessWidget {
  const TeenEducationScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Teen Education Screen")),
    );
  }
}
